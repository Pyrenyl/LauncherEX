<?xml version="1.0" encoding="utf-8"?>
<!--
/*
**
** Copyright 2008, The Android Open Source Project
**
** Licensed under the Apache License, Version 2.0 (the "License");
** you may not use this file except in compliance with the License.
** You may obtain a copy of the License at
**
**     http://www.apache.org/licenses/LICENSE-2.0
**
** Unless required by applicable law or agreed to in writing, software
** distributed under the License is distributed on an "AS IS" BASIS,
** WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
** See the License for the specific language governing permissions and
** limitations under the License.
*/
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
    LauncherEX standalone manifest. It intentionally does not merge the platform Launcher3
    manifests, whose system-only components and permissions are outside this build.
    -->


    <!-- LauncherEX: use a package-specific permission so it can coexist with system Launcher3. -->
    <permission
        android:name="com.android.launcher3.ex.permission.START_WIDGET_PICKER_ACTIVITY"
        android:protectionLevel="signature|privileged" />
    <!--
    The manifest defines the common entries that should be present in any derivative of Launcher3.
    The components should generally not require any changes.

    Rest of the components are defined in AndroidManifest.xml which is merged with this manifest
    at compile time. Note that the components defined in AndroidManifest.xml are also required,
    with some minor changed based on the derivative app.
    -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.SET_WALLPAPER_HINTS" />
    <uses-permission android:name="android.permission.BIND_APPWIDGET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
 LauncherEX: default launchers need this role-granted permission to enumerate the private
         profile; LOCK_APPS is omitted because it is system-only.
    -->
    <uses-permission android:name="android.permission.ACCESS_HIDDEN_PROFILES" />
    <!-- LauncherEX: package-scoped permissions must not collide with the system Launcher3. -->
    <permission
        android:name="com.android.launcher3.ex.permission.GRID_CONTROL"
        android:knownCerts="@array/grid_control_known_signers"
        android:protectionLevel="signature|knownSigner" />

    <uses-permission android:name="com.android.launcher3.ex.permission.GRID_CONTROL" />
    <application
        android:name="com.android.launcher3.LauncherApplication"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:backupAgent="com.android.launcher3.LauncherBackupAgent"
        android:backupInForeground="true"
        android:fullBackupContent="@xml/backupscheme"
        android:fullBackupOnly="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/ic_launcher_home"
        android:label="@string/derived_app_name"
        android:largeHeap="@bool/config_largeHeap"
        android:restoreAnyVersion="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <!--
        Main launcher activity. When extending only change the name, and keep all the
        attributes and intent filters the same
        -->
        <activity
            android:name="com.android.launcher3.Launcher"
            android:clearTaskOnLaunch="true"
            android:configChanges="keyboard|keyboardHidden|mcc|mnc|navigation|orientation|screenSize|screenLayout|smallestScreenSize"
            android:enabled="true"
            android:exported="true"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:resumeWhilePausing="true"
            android:screenOrientation="unspecified"
            android:stateNotNeeded="true"
            android:taskAffinity=""
            android:windowSoftInputMode="adjustPan" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.SHOW_WORK_APPS" />
                <action android:name="android.intent.action.ALL_APPS" />

                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.MONKEY" />
                <category android:name="android.intent.category.LAUNCHER_APP" />
            </intent-filter>
            <!-- LauncherEX: point clients at this package's unique provider authority. -->
            <meta-data
                android:name="com.android.launcher3.grid.control"
                android:value="com.android.launcher3.ex.grid_control" />
        </activity>

        <!-- LauncherEX: protect this exported activity with the renamed package permission. -->
        <activity
            android:name="com.android.launcher3.widgetpicker.WidgetPickerActivity"
            android:autoRemoveFromRecents="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleTop"
            android:permission="com.android.launcher3.ex.permission.START_WIDGET_PICKER_ACTIVITY"
            android:theme="@style/WidgetPickerActivityTheme" >
            <intent-filter>
                <action android:name="android.intent.action.PICK" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <uses-library
            android:name="androidx.window.extensions"
            android:required="false" />
        <uses-library
            android:name="androidx.window.sidecar"
            android:required="false" />
        <!-- Intent received when a session is committed -->
        <receiver
            android:name="com.android.launcher3.SessionCommitReceiver"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.content.pm.action.SESSION_COMMITTED" />
            </intent-filter>
        </receiver> <!-- Intent received used to initialize a restored widget -->
        <receiver
            android:name="com.android.launcher3.AppWidgetsRestoredReceiver"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_HOST_RESTORED" />
            </intent-filter>
        </receiver>

        <!--
        LauncherEX: the system dialer converts *#*#3825#*#* into this protected broadcast.
        Listen only to the modern action because Android emits both modern and legacy actions.
        -->
        <receiver
            android:name="com.android.launcher3.PrivateSpaceSecretCodeReceiver"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.telephony.action.SECRET_CODE" />

                <data
                    android:host="3825"
                    android:scheme="android_secret_code" />
            </intent-filter>
        </receiver>

        <!--
        LauncherEX: handle only the secure-camera action used while the device is locked.
        The no-display activity locks Private Space immediately without replacing normal camera flows.
        -->
        <activity
            android:name="com.android.launcher3.PrivateSpaceLockActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="@string/derived_app_name"
            android:noHistory="true"
            android:showWhenLocked="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay" >
            <intent-filter>
                <action android:name="android.media.action.STILL_IMAGE_CAMERA_SECURE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name="com.android.launcher3.notification.NotificationListener"
            android:exported="true"
            android:label="@string/notification_dots_service_title"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" >
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <meta-data
            android:name="android.nfc.disable_beam_default"
            android:value="true" />

        <activity
            android:name="com.android.launcher3.dragndrop.AddItemActivity"
            android:autoRemoveFromRecents="true"
            android:excludeFromRecents="true"
            android:exported="true"
            android:theme="@style/AddItemActivityTheme" >
            <intent-filter>
                <action android:name="android.content.pm.action.CONFIRM_PIN_SHORTCUT" />
                <action android:name="android.content.pm.action.CONFIRM_PIN_APPWIDGET" />
            </intent-filter>
        </activity>
        <!--
        The settings provider contains Home's data, like the workspace favorites. The permissions
        should be changed to what is defined above. The authorities should also be changed to
        represent the package name.
        -->
        <!-- LauncherEX: use a unique authority so both launcher packages can be installed. -->
        <provider
            android:name="com.android.launcher3.LauncherProvider"
            android:authorities="com.android.launcher3.ex.settings"
            android:exported="true"
            android:readPermission="android.permission.ACCESS_LAUNCHER_DATA"
            android:writePermission="android.permission.ACCESS_LAUNCHER_DATA" />
        <!--
          The content provider for exposing various launcher grid options.
          The caller should hold one of the following permissions:
             - android.permission.BIND_WALLPAPER
             - ${applicationId}.permission.GRID_CONTROL
        -->
        <!-- LauncherEX: keep grid-control IPC inside the renamed package namespace. -->
        <provider
            android:name="com.android.launcher3.graphics.LauncherCustomizationProvider"
            android:authorities="com.android.launcher3.ex.grid_control"
            android:exported="true" />
<!--         The settings activity. To extend point settings_fragment_name to appropriate fragment class -->
        <activity
            android:name="com.android.launcher3.settings.SettingsActivity"
            android:autoRemoveFromRecents="true"
            android:exported="true"
            android:icon="@drawable/ic_setting"
            android:label="@string/settings_button_text"
            android:theme="@style/HomeSettings.Theme" >
            <intent-filter>
                <action android:name="android.intent.action.APPLICATION_PREFERENCES" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
<!--         Launcher activity for secondary display -->
        <activity
            android:name="com.android.launcher3.secondarydisplay.SecondaryDisplayLauncher"
            android:enabled="true"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.SECONDARY_HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <property
            android:name="android.window.PROPERTY_ACTIVITY_EMBEDDING_SPLITS_ENABLED"
            android:value="true" />

        <activity
            android:name="com.android.launcher3.organizer.creation.screen.ui.OrganizerActivity"
            android:autoRemoveFromRecents="true"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@style/OrganizerTheme" >
            <intent-filter>
                <action android:name="com.android.launcher3.ORGANIZER" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.android.launcher3.organizer.creation.screen.ui.foldercreator.FolderCreatorActivity"
            android:autoRemoveFromRecents="true"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@style/FolderCreatorTheme" >
            <intent-filter>
                <action android:name="com.android.launcher3.FOLDER_CREATION" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name="androidx.slice.compat.SlicePermissionActivity"
            android:excludeFromRecents="true" >
        </activity>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
